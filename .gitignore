# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Django
db.sqlite3
data/
staticfiles/

# Local environment. Holds secrets, so it must never be committable by accident.
.envrc

# Invoices and their PDFs are real documents about real people; the working tree is not
# where they belong.
*.pdf

# Tailwind
tailwindcss
